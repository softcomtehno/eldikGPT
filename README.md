# Банковский ИИ Ассистент

Frontend приложение для банковского ИИ ассистента с возможностью создания ассистентов, ведения чатов и просмотра истории.

## Функциональность

- 📋 **Список ассистентов** - просмотр всех доступных ИИ ассистентов
- ➕ **Создание/редактирование ассистентов** - модальное окно с полями:
  - Название
  - Описание
  - Фото (загрузка изображения)
  - Файл с настройками (необязательно, JSON/TXT/YAML)
- 💬 **Чат в реальном времени** - общение с ассистентом с имитацией ответов
- 📜 **История чатов** - просмотр всех предыдущих разговоров

## Технологии

- **React** - UI библиотека
- **Vite** - сборщик проекта
- **Tailwind CSS** - стилизация
- **NextUI** - компоненты UI
- **React Router** - маршрутизация
- **Lucide React** - иконки

## Установка

1. Установите зависимости:

```bash
npm install
```

2. Запустите dev сервер:

```bash
npm run dev
```

3. Откройте браузер по адресу `http://localhost:5173`

## Структура проекта

```
src/
├── components/          # Переиспользуемые компоненты
│   ├── Layout.jsx      # Основной layout с навигацией
│   └── AssistantModal.jsx  # Модальное окно создания/редактирования
├── pages/              # Страницы приложения
│   ├── HomePage.jsx    # Главная страница со списком ассистентов
│   ├── ChatPage.jsx    # Страница чата
│   └── ChatHistoryPage.jsx  # Страница истории чатов
├── data/               # Mock данные
│   └── mockData.js     # JSON данные для ассистентов и чатов
├── App.jsx             # Главный компонент с роутингом
└── main.jsx            # Точка входа
```

## Данные

Приложение использует mock данные из `src/data/mockData.js`. Для подключения к реальному бэкенду:

1. Замените импорты mock данных на API вызовы
2. Используйте состояние для управления данными
3. Добавьте обработку загрузки и ошибок

## Сборка для продакшена

```bash
npm run build
```

Собранные файлы будут в папке `dist/`.

